import { SwipeDirectiveDirective } from './swipe-directive.directive';

describe('SwipeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
